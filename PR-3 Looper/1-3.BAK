#include <stdio.h>
#include <conio.h>


main ()
{
	int i, first, last;

	clrscr();

	printf("\n\tEnter any Number :");
	scanf("%d", &i);

	last = i % 10;

	for (; i != 0; i = i / 10)
	{
		first = i;
	}

	printf("\n\tThe sum of the first and last digi : %d", first + last);

	getch();
}