#include <stdio.h>
#include <conio.h>

main()
{
	float c, F;

	clrscr();

	printf("Enter Temprature in Celsius\t :");
	scanf("%f",&c);

	F = (9.0 / 5.0 * c) + 32;
	printf("The temprature in Fahrenheit = %.2f", F);

	getch();
}